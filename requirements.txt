# Pinned to the versions this app is actually developed and tested
# against, so a Render build gets the same packages that work locally.
#
# Render start command:  gunicorn app:server
# app.py exposes `server = app.server` for exactly this.

dash==4.4.1
dash-bootstrap-components==2.0.4
pandas==3.0.5
numpy==2.5.2
plotly==6.9.0
requests==2.34.2

# WSGI server used in deployment only -- not needed to run locally with
# `python app.py`, which uses Dash's built-in dev server instead.
gunicorn==23.0.0
